# SkySolver v2 Dependencies

# Rules engine & solvers
pydantic>=2.0
pyomo>=6.4  # MILP formulation for Tier 2
highspy>=1.8  # Embedded HiGHS runtime used by the Railway hackathon deployment

# API & UI
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6
websockets>=12.0

# Durable production state and Amazon MSK transport
psycopg[binary,pool]>=3.2
confluent-kafka>=2.6
aws-msk-iam-sasl-signer-python>=1.0.2
boto3>=1.35
PyJWT[crypto]>=2.10

# Stream processing (production)
# flink>=1.17  # Apache Flink for stateful event processing

# Observability
prometheus-client>=0.17  # Metrics export
opentelemetry-api>=1.20  # Tracing
opentelemetry-sdk>=1.20

# Testing
pytest>=7.4
pytest-asyncio>=0.21
httpx2>=2.9  # Starlette/FastAPI contract and ASGI tests

# Utilities
numpy>=1.24  # Numerical operations
pandas>=2.0  # Data manipulation
python-dotenv>=1.0  # Config management
